# Onion Architecture API (Clean Architecture)

[![FastAPI](https://img.shields.io/badge/FastAPI-005571?style=for-the-badge&logo=fastapi)](https://fastapi.tiangolo.com)
[![Python](https://img.shields.io/badge/Python-3.9+-blue?style=for-the-badge&logo=python)](https://python.org)

Этот проект представляет собой **практический шаблон (boilerplate) луковой архитектуры (Onion Architecture)** на базе FastAPI. Он демонстрирует, как построить гибкое, тестируемое и легко поддерживаемое приложение, следуя принципам Чистой архитектуры (Clean Architecture) Роберта Мартина.

## 🧅 Что такое Луковая архитектура?

Луковая архитектура — это подход к организации кода, который ставит основную бизнес-логику (домен) в центр приложения, а все внешние зависимости (базы данных, API, UI) располагаются на внешних "слоях" или "кольцах", окружающих ядро.

Основная идея "луковицы" заключается в **строгом правиле зависимостей**: зависимости всегда должны быть направлены **внутрь**, от внешних слоев к внутренним. Код во внутренних слоях ничего не знает о коде во внешних слоях. Это достигается за счет использования интерфейсов (абстракций).

```text
    [ Внешний слой: Presentation (API) ]
     → [ Слой: Infrastructure (БД, внешние сервисы) ]
      → [ Слой: Application (Сценарии использования) ]
       → [ Ядро: Domain (Бизнес-сущности и правила) ]
```

Луковая архитектура объединяет принципы чистой архитектуры (слои) и гексагональной (порты/адаптеры), обеспечивая максимальную гибкость и тестируемость системы.

## 🗂️ Структура проекта
```text
my_blog_project_onion/
├── app/
│ ├── init.py
│ ├── main.py # Главный файл FastAPI, инициализация роутеров
│ │
│ ├── presentation/ # 🔴 САМЫЙ ВНЕШНИЙ СЛОЙ: Презентационный (API)
│ │ ├── init.py
│ │ ├── dependencies.py # Зависимости для презентационного слоя
│ │ └── routers/ # APIRouters (эндпоинты)
│ │ ├── init.py
│ │ ├── categories.py
│ │ └── posts.py
│ │
│ ├── infrastructure/ # 🟠 СЛОЙ: Инфраструктурный
│ │ ├── init.py
│ │ ├── persistence/ # Реализации хранилищ данных
│ │ │ ├── init.py
│ │ │ └── sqlalchemy/ # Конкретная реализация на SQLAlchemy
│ │ │ ├── init.py
│ │ │ ├── category_repository.py
│ │ │ └── post_repository.py
│ │ └── database/ # Настройка подключения к БД
│ │ ├── init.py
│ │ ├── dependencies.py
│ │ └── connection.py
│ │
│ ├── application/ # 🟡 СЛОЙ: Приложения (Use Cases)
│ │ ├── init.py
│ │ ├── services/ # Сервисы, реализующие сценарии использования
│ │ │ ├── init.py
│ │ │ ├── category_service.py
│ │ │ └── post_service.py
│ │ └── schemas/ # Pydantic схемы (DTO)
│ │ ├── init.py
│ │ ├── category.py
│ │ └── post.py
│ │
│ ├── domain/ # 🟢 ЯДРО: Доменный слой
│ │ ├── init.py
│ │ ├── models/ # Бизнес-сущности
│ │ │ ├── init.py
│ │ │ ├── category.py
│ │ │ └── post.py
│ │ └── repositories/ # Абстрактные интерфейсы (порты)
│ │ ├── init.py
│ │ ├── categories.py # ICategoryRepository (интерфейс)
│ │ └── posts.py # IPostRepository (интерфейс)
│ │
└── requirements.txt
```


## 🎯 Слои архитектуры (изнутри наружу)

### 1. 🟢 Доменный слой (Domain) — Ядро

**Самое сердце приложения.** Содержит бизнес-правила и модели предметной области. Этот слой совершенно независим от инфраструктуры.

*   **`domain/models/`**: Бизнес-сущности (`Category`, `Post`). Чистые Python-классы.
*   **`domain/repositories/`**: **Абстрактные интерфейсы (порты)**. Определяют *что* можно делать с данными (`get_by_id`, `create`), но не *как*. (Например, `ICategoryRepository`).

> ✅ **Правило:** Никаких зависимостей вовне. Только чистая бизнес-логика.

### 2. 🟡 Слой приложения (Application)

**Оркестрирует бизнес-правила для выполнения конкретных задач.** Содержит сценарии использования (use cases).

*   **`application/services/`**: Сервисы (`CategoryService`, `PostService`), реализующие сценарии использования. Используют интерфейсы репозиториев из Domain.
*   **`application/schemas/`**: Pydantic схемы (DTO). Определяют контракты ввода/вывода для API, отделяя внешний мир от внутренних моделей.

> ✅ **Зависит от:** Domain слоя.

### 3. 🟠 Инфраструктурный слой (Infrastructure)

**Содержит детали реализации.** Здесь живут конкретные технологии: SQLAlchemy, внешние API и т.д.

*   **`infrastructure/persistence/sqlalchemy/`**: **Конкретные реализации репозиториев** (адаптеры), выполняющие интерфейсы из `domain/repositories/`. Например, `SQLAlchemyCategoryRepository` реализует `ICategoryRepository`.
*   **`infrastructure/database/`**: Настройка подключения к БД и DI-контейнер.

> ✅ **Зависит от:** Domain слоя (реализует его интерфейсы) и внешних библиотек.

### 4. 🔴 Презентационный слой (Presentation) — Самый внешний

**Взаимодействует с пользователем.** В нашем случае — FastAPI HTTP API.

*   **`presentation/routers/`**: Эндпоинты API. Получают запрос, валидируют через Pydantic (из `application/schemas/`), вызывают сервисы из Application слоя.
*   **`presentation/dependencies.py`**: Фабрики зависимостей FastAPI для внедрения репозиториев и сервисов.

> ✅ **Зависит от:** Application слоя (сервисов и схем).

## 🔄 Поток данных (Как это работает)

1.  **Запрос** приходит в `Presentation` (роутер `/categories`).
2.  Роутер через `Depends()` получает **сервис** из `Application` слоя.
3.  Сервис применяет бизнес-логику и обращается к данным через **интерфейс репозитория** (из `Domain`).
4.  `Infrastructure` слой предоставляет **конкретную реализацию** репозитория (например, SQLAlchemy).
5.  Репозиторий идет в **базу данных**, получает данные и возвращает **доменную модель**.
6.  Сервис возвращает результат роутеру.
7.  Роутер преобразует модель в **DTO (Pydantic схему)** и отдает **HTTP-ответ**.

## ✨ Преимущества (зачем это нужно)

| Преимущество | Описание |
| :--- | :--- |
| **🧪 Тестируемость** | Каждый слой тестируется изолированно. Можно замокать репозиторий и тестировать сервисы без БД. |
| **🔄 Гибкость** | Хотите сменить SQLAlchemy на SQLModel или перейти с PostgreSQL на MongoDB? Меняете только `Infrastructure` слой, ядро не трогаете. |
| **🔌 Независимость от фреймворков** | Бизнес-логика ничего не знает о FastAPI или SQLAlchemy. Ее можно перенести на любой другой фреймворк. |
| **👥 Поддержка** | Четкое разделение ответственности упрощает работу в команде и поддержку кода. |

## 🚀 Быстрый старт

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/MaminAmetist/onion.git
    cd onion
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    python -m venv venv
    source venv/bin/activate  # Linux/Mac
    # venv\Scripts\activate   # Windows
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Запустите сервер:**
    ```bash
    uvicorn app.main:app --reload
    ```

5.  **Изучите документацию API:**
    Перейдите по адресу [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

## 📖 Заключение

Этот проект демонстрирует, как построить **сложное, но гибкое приложение на FastAPI**, следуя лучшим практикам луковой архитектуры. Он идеально подходит для средних и крупных проектов, где важна долгосрочная поддержка, тестируемость и независимость от технологий.

Ключевой принцип, отраженный в структуре: **зависимости направлены внутрь, а бизнес-логика находится в центре и ничего не знает о внешнем мире.**